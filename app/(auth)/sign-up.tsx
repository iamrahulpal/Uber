import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const SignIn = () => {
  return (
    <SafeAreaView>
      <Text className=" text-blue-500 text-lg font-JakartaBold ">Sign up</Text>
    </SafeAreaView>
  );
};
export default SignIn;
