import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Home = () => {
  return (
    <SafeAreaView>
      <Text className=" text-blue-500 text-lg font-JakartaBold ">Hello World!</Text>
    </SafeAreaView>
  );
};
export default Home;
