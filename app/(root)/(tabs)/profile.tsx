import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Profile = () => {
  return (
    <SafeAreaView>
      <Text className=" text-blue-500 text-lg font-JakartaBold ">Profile</Text>
    </SafeAreaView>
  );
};
export default Profile;
